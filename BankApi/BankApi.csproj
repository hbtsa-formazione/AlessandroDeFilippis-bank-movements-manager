<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- Versione del framework .NET da utilizzare. net8.0 è una versione recente e stabile (LTS) -->
    <TargetFramework>net8.0</TargetFramework>
    <!-- Abilita il controllo dei null per evitare errori comuni (NullReferenceException) -->
    <Nullable>enable</Nullable>
    <!-- Abilita gli import globali comuni (System, System.Linq, etc.) per scrivere meno codice -->
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="8.0.2" />
  </ItemGroup>

</Project>
